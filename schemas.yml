components:
  schemas:
    Error:
      type: object
      properties:
        message:
          type: string
          example: Internal server error
      required:
        - message

    UrlPostOneBody:
      type: object
      properties:
        url:
          type: string
          example: https://www.youtube.com
      required:
        - url
